#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int t, n, i, j, k;
    cin >> t;
    for (i = 0; i < t; i++) {
        cin >> n >> k;
        int l[n];
        int r[n];
        for (j = 0; j < n; j++) {
            cin >> l[j];
        }
        for (j = 0; j < n; j++) {
            cin >> r[j];
        }
    }

    return 0;
}